// HTML/CSS parser for html-to-figma-extension
// Parses HTML/CSS to Figma-compatible structure
function parseHTML(html) {
  // Placeholder: parse HTML string to Figma format
  return {};
}

function parseCSS(css) {
  // Placeholder: parse CSS string to Figma format
  return {};
}

module.exports = { parseHTML, parseCSS };