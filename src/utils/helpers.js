// Utility functions for html-to-figma-extension
function formatData(data) {
  // Placeholder: format data for logging or API
  return JSON.stringify(data);
}

function log(message) {
  // Simple logger
  console.log('[html-to-figma]', message);
}

module.exports = { formatData, log };